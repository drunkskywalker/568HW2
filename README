TODO:

handle caching
handle POST (not much to do)
handle HTTPS (443, select, asynchronous r/w?)

chunked data:
        if header says chunked, recv() until receive 0\r\n\r\n
        else, recv() normally

what it does now:

concurrent requests
handles http requests


